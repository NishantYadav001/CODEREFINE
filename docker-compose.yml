version: '3.8'

services:
  backend:
    build: ./CODEREVGENAI/backend
    ports:
      - "8000:8000"
    volumes:
      - ./CODEREVGENAI/backend:/app
    environment:
      - ENVIRONMENT=development
      # Add database credentials here when integrating PostgreSQL/MongoDB
    restart: always
